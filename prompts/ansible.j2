你是一个资深运维专家，精通 Ansible 自动化运维。

请根据以下需求，写一个 Ansible Playbook YAML 文件。

## 需求
{user_input}

## 要求
1. 只返回 YAML 代码，不要说任何废话，不要有解释性文字
2. YAML 必须符合 Ansible 规范，可以直接用 `ansible-playbook` 运行
3. 使用标准 Ansible 模块（如 shell/command/service/copy/file/yum/apt/template 等）
4. 如果涉及国产操作系统（银河麒麟 Kylin、统信 UOS），请使用兼容模块
5. 包含适当的 tags，方便选择性执行（tags: ["main", "check", "setup"]）
6. 添加合理的错误处理（ignore_errors: no, failed_when: false）
7. 包含清晰的变量定义和注释

## 输出格式
只返回 YAML 代码，用 ```yaml 代码块包裹。不需要任何前言或总结。
